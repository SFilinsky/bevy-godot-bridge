## Idea

Nodes that enable feature systems and group importing/exporting APIs. It will mean that APIs and feature are only enabled when the node is attached to the Godot Scene, specifically BevyAppSingleton.

